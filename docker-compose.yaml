version: '3.8'
services:
  redis:
    image: redis:7-alpine
    container_name: redis_service
    ports:
      - "6379:6379"
    restart: always

  # Позже добавим сюда сервис с самим приложением и БД